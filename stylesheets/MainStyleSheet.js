import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
      marginTop: 5, 
      alignItems: "center",
      },

      welcomeText: {
      fontSize: 41,
      color: "#cc0000",
    }
    
  })

export default styles