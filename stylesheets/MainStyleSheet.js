import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
      marginTop: 5, 
      alignItems: "center",
      },

    welcomeText: {
      fontSize: 40,
      color: "#cc0000",
      },

    buttonContainer: {
      marginTop: 15, 
      alignItems: "center",
      },

    button: {
      color: "#cc0000",
      }
    
  })

  
export default styles